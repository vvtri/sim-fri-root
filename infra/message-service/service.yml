---
kind: Service
apiVersion: v1
metadata:
  name: message-service
spec:
  selector:
    app: message-service
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
